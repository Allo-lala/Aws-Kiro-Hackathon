import { IUserTracker } from '../services/interfaces/IUserTracker';

export class UserController {
  constructor(private userTracker: IUserTracker) {}

  // API endpoint implementations will be added in subsequent tasks
}