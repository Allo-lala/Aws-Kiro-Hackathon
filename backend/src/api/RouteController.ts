import { IRoutePlanner } from '../services/interfaces/IRoutePlanner';

export class RouteController {
  constructor(private routePlanner: IRoutePlanner) {}

  // API endpoint implementations will be added in subsequent tasks
}