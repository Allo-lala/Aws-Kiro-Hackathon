import { IRealtimeUpdater } from '../services/interfaces/IRealtimeUpdater';

export class RealtimeController {
  constructor(private realtimeUpdater: IRealtimeUpdater) {}

  // API endpoint implementations will be added in subsequent tasks
}